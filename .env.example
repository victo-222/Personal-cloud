# Copy this to .env and fill in your values
# OpenAI / LLM settings
OPENAI_API_KEY=your_openai_api_key_here
# Optional: change model (defaults to gpt-4o-mini in the proxy)
OPENAI_MODEL=gpt-4o-mini
# Optional: a custom API URL
OPENAI_API_URL=

# Other envs used by your app (example)
# VITE_SUPABASE_URL=
# VITE_SUPABASE_ANON_KEY=

# SMTP / transactional email (for production email delivery)
# Example for SendGrid SMTP
# SMTP_HOST=smtp.sendgrid.net
# SMTP_PORT=587
# SMTP_USER=apikey
# SMTP_PASS=your_sendgrid_api_key_here
# SENDER_EMAIL=review@gmail.com

# If you don't set SMTP_* envs, the dev endpoint will use Ethereal for testing and return a preview URL.
